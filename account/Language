You are a senior full-stack and mobile application engineer.

I have an existing **web-based project (frontend)** with a complete folder structure, design system, components, pages, styles, and modules.

Your task is to **create a full mobile version of this project** in a **separate folder**, while preserving the same:

* UI structure
* Design system (colors, typography, spacing)
* Component hierarchy
* Business logic (where applicable)
* Module organization

---

### 🔧 REQUIREMENTS

1. **Project Setup**

* Create a new folder: `/mobile-app`
* Use **React Native (Expo preferred)** or **React Native CLI**
* Setup a clean, scalable architecture

---

### 📁 Folder Structure (must mirror web structure)

Replicate the same structure as the web project:

* `/components`
* `/screens` (instead of pages)
* `/modules`
* `/services`
* `/hooks`
* `/store`
* `/utils`
* `/assets`
* `/theme`

Keep naming consistent with the web version.

---

### 🎨 UI / DESIGN SYSTEM

* Extract all colors, fonts, spacing, and reuse them in `/theme`
* Maintain exact color palette
* Convert CSS/SCSS/Tailwind styles into React Native StyleSheet
* Ensure consistent typography and spacing

---

### 📱 MOBILE ADAPTATION RULES

* Convert all web pages into mobile screens
* Replace:

  * `div` → `View`
  * `button` → `TouchableOpacity` / `Pressable`
  * `input` → `TextInput`
* Use `ScrollView` where necessary
* Implement proper responsive layouts (flexbox)

---

### 🔄 NAVIGATION

* Use **React Navigation**
* Convert:

  * Web routes → Stack / Tab navigation
* Maintain same navigation flow as web

---

### 🧠 STATE MANAGEMENT

* Reuse existing logic (Redux / Zustand / Context)
* Adapt API calls for mobile environment
* Ensure authentication flow works

---

### 🌙 DARK MODE SUPPORT

* Implement global dark/light mode toggle
* Use theme provider
* Sync with system preference if possible

---

### 🔐 FORMS & VALIDATION

* Convert all forms properly
* Maintain validations
* Ensure mobile-friendly input UX

---

### 🌐 API INTEGRATION

* Reuse existing API services
* Ensure base URL config works on mobile
* Handle errors properly

---

### ⚠️ IMPORTANT FIXES

* Fix any UI that breaks on small screens
* Optimize performance (avoid heavy re-renders)
* Remove web-only dependencies

---

### 🧪 FINAL STEP

After implementation:

1. Scan the entire mobile project
2. Detect:

   * UI inconsistencies
   * Broken navigation
   * Unused components
   * Performance issues
3. Fix all issues
4. Output a summary report of:

   * What was created
   * What was adapted
   * What was fixed

---

### 🎯 GOAL

Deliver a **production-ready mobile app** that looks and behaves like the web version but is fully optimized for mobile devices.

Do NOT skip any module or screen.

---
