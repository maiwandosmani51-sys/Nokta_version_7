## TASK: Remove all profile images from Nokta Academy Management System

Remove ALL profile image functionality completely from the entire project. Delete image fields from database schemas, remove image upload from all forms, remove image display from all cards/tables/profiles, remove image generation from seed script, and remove image caching from service worker. Keep ALL other functionality intact (authentication, RBAC, CRUD, offline support, PWA, all data operations).

### Files to modify exactly:

**Backend Models:**
- backend/src/models/User.ts → Delete the 'profileImage' field from the schema entirely
- backend/src/models/Student.ts → Delete 'profileImage' field if exists
- backend/src/models/Teacher.ts → Delete 'profileImage' field if exists

**Backend Modules:**
- backend/src/modules/students.ts → Remove ALL 'profileImage' references from GET responses, POST/PUT handlers, and validation schemas
- backend/src/modules/teachers.ts → Remove ALL 'profileImage' references from GET responses, POST/PUT handlers, and validation schemas  
- backend/src/modules/users.ts → Remove ALL 'profileImage' references

**Backend Seed:**
- backend/src/seed.ts → Delete generateLocalProfileImage() function calls, remove 'profileImage' from all user creation objects, remove any fs/file operations for images

**Frontend Config:**
- frontend/src/config/modules.ts → Remove 'profileImage' from fields array in teachers, students, and users module definitions

**Frontend Components:**
- frontend/src/modules/CrudPage.tsx → Remove image preview in forms, remove image column from tables, remove file upload input, remove image URL rendering
- frontend/src/modules/ProfilePage.tsx → Remove profile image display and image upload button
- frontend/src/modules/dashboard/DashboardPage.tsx → Remove avatar/image display from user cards
- frontend/src/components/ui/Card.tsx → Remove any image-related props and rendering

**Frontend Services:**
- frontend/src/services/api.ts → Change any multipart/form-data to application/json, remove image upload handling

**Service Worker:**
- public/sw.js → Remove '/uploads/*' from cache URL patterns

### What to keep:
Keep ALL other fields: name, email, phone, fatherName, salaryType, salaryValue, fee, teacherId, classId, subjectId, rollNo, active status, createdAt, updatedAt, and all relationships.

Keep ALL functionality: authentication, login, dashboard, CRUD operations for all modules (students, teachers, classes, subjects, exams, results, families, books, expenses, notifications), RBAC with 7 roles, ACL, offline mode, PWA, lazy loading, React Query caching, Zustand store, audit logging.

### After changes:
The system should work without errors. No image fields should appear in any form, card, table, or API response. The backend should run on port 8081, frontend on port 5173. Database seeding should complete without image generation errors.

Execute all changes now.